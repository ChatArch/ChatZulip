# 开发计划

这个页面记录 `ChatZulip` 的文档化路线。模板只提供结构；请按真实实现和验证进展更新。

## Review Contract

- CLI 命令必须调用可 import 的 Python API。
- 文档先写已实现和已验证能力；未实现能力必须标记为未实现。
- 敏感信息不得进入 README、docs、issue、PR 评论或 CI log。
- 破坏性操作默认 dry-run，或要求显式 `--apply`。

## Phase 1：当前已实现能力

```text
待项目实现后补充。
```

## Phase 2：下一步计划

```text
待项目确认后补充。
```
